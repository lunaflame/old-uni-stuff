package level;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class CollisionTest {

	@BeforeEach
	void setUp() throws Exception {
	}

	@AfterEach
	void tearDown() throws Exception {
	}

	@Test
	void test() {
		/*BaseLevel lv = new BaseLevel();
		BaseCell woll = new BaseCell();
		woll.setCanCollide(true);
		
		BaseCell notWoll = new BaseCell();
		notWoll.setCanCollide(false);
	
		for (int x = 0; x < 3; x++) {
			for (int y = 0; y < 3; y++) {
				woll.setPos(new Point2D(x, y));
				lv.addCell(woll);
			}
		}
		
		notWoll.setPos(new Point2D(1, 1));
		lv.addCell(notWoll);
		
		Rectangle2D hbox = new Rectangle2D(1.25, 1.25, 0.5, 0.5);
		boolean i1 = lv.Collision.collideWalls(hbox);
		
		Rectangle2D hbox2 = new Rectangle2D(1.5, 1.5, 0.5, 0.5);
		boolean i2 = lv.Collision.collideWalls(hbox2);
		
		Rectangle2D hbox3 = new Rectangle2D(1.75, 1.75, 0.5, 0.5);
		boolean i3 = lv.Collision.collideWalls(hbox3);
		
		System.out.println("intersections: " + i1 + ", " + i2 + ", " + i3);*/
	}

}
